# music/

Drop audio files here (mp3/m4a/flac/ogg/wav/opus), then reference them from `manifest.json`'s
`music` list (`file` should be `music/yourfile.mp3`) along with `title`/`artist`/`album`.
Only add music you have the rights to distribute — it downloads straight into anyone's library
who taps it in the app.
